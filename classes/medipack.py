class MediPack:
    def __init__(self, sprite, x_pos, y_pos, health):
        self.sprite = sprite
        self.x_pos = x_pos
        self.y_pos = y_pos
        self.health = health

    def apply_effect(self, player):
        pass